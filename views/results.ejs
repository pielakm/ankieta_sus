<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>Wszystkie Wyniki SUS</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Wszystkie Wyniki Ankiet SUS</h1>

    <a href="/">&#8592; Wróć do Ankiety</a>

    <% if (results.length === 0) { %>
        <p>Brak zapisanych wyników.</p>
    <% } else { %>
        <table>
            <thead>
                <tr>
                    <th>Lp.</th>
                    <th>Pseudonim</th> <th>Data i Czas</th>
                    <% questions.forEach((q, i) => { %>
                        <th>P<%= i + 1 %></th>
                    <% }); %>
                    <th class="sus-score-cell">WYNIK SUS</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach((result, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= result.nickname %></td> <td><%= result.timestamp %></td>
                    <% result.responses.forEach(resValue => { %>
                        <td><%= resValue %></td>
                    <% }); %>
                    <td class="sus-score-cell"><%= result.susScore %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <div class="actions-row">
            <a href="/export-excel" class="btn btn-excel">📊 Pobierz jako Excel (.xlsx)</a>
            <a href="/export" class="btn btn-csv">📄 Pobierz jako CSV</a>
            <a href="/statistic" class="btn btn-statistic">📈 Zobacz Statystyki</a>
        </div>
    <% } %>
    <footer>
        Ankieta SUS została stworzona przez Mateusza Pielaka na potrzeby projektu gry Mahjong.
    </footer>
</body>
</html>